@page
@model SmukToolsApp.Pages.ToolTypes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ToolType[0].Code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ToolType[0].Titel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ToolType[0].Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ToolType[0].PriceDuration)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ToolType[0].RequiresApproval)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.ToolType) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Titel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PriceDuration)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RequiresApproval)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
