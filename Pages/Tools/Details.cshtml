@page
@model SmukToolsApp.Pages.Tools.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Tool</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Tool.Title)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Tool.Title)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Tool.isComplete)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Tool.isComplete)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Tool.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
